version: "3.9"

services:
  chandoeats-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chandoeats-backend
    ports:
      - "8080:8080"
    environment:
      # Override any application.properties values via env vars
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL:-jdbc:postgresql://ep-super-sun-amge4s22-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require}
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME:-neondb_owner}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD:-npg_Td7mF2tIcLrb}
      SPRING_MAIL_USERNAME: ${SPRING_MAIL_USERNAME:-addankichandrahasbabu@gmail.com}
      SPRING_MAIL_PASSWORD: ${SPRING_MAIL_PASSWORD:-ubyrvaksnpqneqqg}
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      APP_SEED_FOOD_ENABLED: "false"
    restart: unless-stopped
