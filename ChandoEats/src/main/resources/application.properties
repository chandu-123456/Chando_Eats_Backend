# spring.application.name=ChandoEats


# #spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:food_db}
# # spring.datasource.url=jdbc:mysql://localhost:3306/chandoeats_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# # spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# # spring.datasource.username=root
# # spring.datasource.password=Chandu@2025
# # spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


# # spring.jpa.hibernate.ddl-auto=update
# # spring.jpa.show-sql=true

# spring.datasource.url=jdbc:postgresql://ep-super-sun-amge4s22-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require
# spring.datasource.username=neondb_owner
# spring.datasource.password=npg_Td7mF2tIcLrb
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.show-sql=true

# spring.datasource.hikari.maximum-pool-size=5
# spring.datasource.hikari.minimum-idle=1

# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=addankichandrahasbabu@gmail.com
# spring.mail.password=ubyrvaksnpqneqqg
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# # Food seeding for local bulk data setup
# app.seed.food.enabled=true
# app.seed.food.count=500


spring.application.name=ChandoEats

# PostgreSQL (replace with your actual values)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_DATASOURCE_USERNAME}
spring.mail.password=${SPRING_DATASOURCE_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Food seeding
app.seed.food.enabled=false
app.seed.food.count=500
